@model IEnumerable<LetsRaid.Models.GuildModels.Guild>

@{
    ViewBag.Title = "Dalaran";
}

<h2>Dalaran</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Server.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Server.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GuildId }) |
            @Html.ActionLink("Details", "Details", new { id=item.GuildId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GuildId })
        </td>
    </tr>
}

</table>
